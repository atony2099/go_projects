
# 1. key 
# 2. build

name: deploy
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: set up go
        uses: actions/setup-go@v4
        with:
          go-version: ^1.20.1'

      - name: Install dependencies 
        run: go mod download 


      - name: Build app
        run: go build -o dist/telbot ./telebot 


    # add key 
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: unnecessary
      - name: Adding Known Hosts
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }}  >> ~/.ssh/known_hosts

      - name: Deploy with rsync
        run:   sync -avz ./dist/* ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/var/www/api



        # - name: Restart system server 
        # uses: appleboy/ssh-action@master
        # with:
        #   host: ${{ secrets.SSH_HOST }}
        #   username: ${{ secrets.SSH_USER }}
        #   key: ${{ secrets.SSH_PRIVATE_KEY }}
        #   port: ${{ secrets.SSH_PORT }}
        #   script: |
        #     cd /var/www/zellwk.com
        #     git fetch origin master
        #     git reset --hard FETCH_HEAD
        #     git clean -d -f --exclude secrets
        #     npm install --production
        #     chown $(whoami) . # PM2 doesn't recognize root user from Github Actions
        #     npm run restart

      